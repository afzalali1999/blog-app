<%- include('partials/header') %>

<div class="ui main text container segment">
    <div class="ui huge header">Edit <%= blog.title %></div>
    
    <!--In our form, everything is inside of Blog and a grouped together. If you do req.body.blog it automatically has title, image, and body-->
    <!--html forms don't support anything other than a GET or POST request ?_method=PUT will send a regular post request, and method override will configure it inside of app.js, so that it looks for _method, and if it sees that underscore method equals to PUT or DELETE, it will treat it as a PUT request or as a DELETE request, hence the name method override// You have to do npm install method-override -save , and then you have to require method-override in app.js-->
    <form class="ui form" action="/blogs/<%= blog._id %>?_method=PUT" method="POST">
        <div class="field">
            <label>Title</label>
            <!--We specify value inside the form whatever is inside of the input using the value attribute. Ex: value="Hello" will write and prefill hello in the form when it loads-->
            <input type="text" name="blog[title]" value="<%= blog.title %>">
        </div>
        <div class="field">
            <label>Image</label>
            <input type="text" name="blog[image]" value="<%= blog.image %>">
        </div>
        <div class="field">
            <label>Blog Content</label>
            <!--textarea does not have a value attribute. Instead, whatever is in between the tags, the inner content, is what is prefilled-->
            <textarea name="blog[body]"><%= blog.body %></textarea>
        </div>
        <button class="ui violet big basic button">Update</button>
    </form>
</div>

<%- include('partials/footer') %>